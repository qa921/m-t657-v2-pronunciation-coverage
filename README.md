# M-T657-V2 Pronunciation Coverage

Source-state fixture for an audio-focused language-learning release. Audit `data/curriculum.json`, `data/stale-audio-manifest.json`, and `public/audio/`. `data/learner-progress-prior-state.json` is a prior-state export representing the learner persistence contract; no runtime manifest endpoint exists yet.

The supplied WAVs are intentionally tiny fixture recordings. Missing files are intentional and must remain distinguishable from playback failures.
